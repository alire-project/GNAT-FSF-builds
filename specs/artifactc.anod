from e3.anod.loader import spec
from e3.os.fs import touch
from e3.anod.spec import Anod
import os

class ArtifactB(spec('gh-artifact')):

    @property
    def version(self):
        return "3.0.0"

    @property
    def build_deps(self):
        return [Anod.Dependency("artifactb")]

    def artifact_build(self):
        touch(os.path.join(self['INSTALL_DIR'], "artifact_c_1"))
        touch(os.path.join(self['INSTALL_DIR'], "artifact_c_2"))
